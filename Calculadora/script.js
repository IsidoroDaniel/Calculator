const calculator = document.querySelector(‘.calculadora-corpo`)
const keys = calculator.querySelector(‘.calculadora-corpo-botoes’)

keys.addEventListener(‘click’, e => {
 if (e.target.matches(‘button’)) {
   // Fazer algo
 }
})